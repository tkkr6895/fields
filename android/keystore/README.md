# Sideload signing identity

`fields-sideload.p12` is the **stable sideload key** for GitHub-built APKs.

It is not a Play Store upload key. Installs from this identity replace each other.
If a previous **debug-signed** Fields app is on the phone, uninstall it first, then install `Fields.apk`.
