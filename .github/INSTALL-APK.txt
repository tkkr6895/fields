GitHub wraps this folder in a zip. Extract it, then install Fields.apk (not the zip).

Sideloading needs unknown sources. Grant precise location (background location if recording with the screen off).

If Android rejects the install, an older Fields build signed with a different key is still on the device — uninstall it, then install this APK.
